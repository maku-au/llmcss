import { WireframeTemplate, PageBlueprint } from './schema';

export const wireframeTemplates: WireframeTemplate[];
export const pageBlueprints: PageBlueprint[];
export function assembleBlueprintHtml(blueprintId: string): string | null;
